query getAllTransactions($vaultId: String!) {
  getAllTransactions(vaultId: $vaultId) {
    id
    accrualDate
    subject
    description
    amount
    count
    status
    tags {
      id
      name
    }
  }
}
