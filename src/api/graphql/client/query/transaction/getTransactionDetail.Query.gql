query getTransactionDetail($transactionId: String!) {
  getTransactionDetail(transactionId: $transactionId) {
    id
    accrualDate
    subject
    description
    amount
    count
    status
    tags {
      id
      name
    }
  }
}
