import { AddAccountFrom } from '@/components/account'
import { PageHeader, Layout } from '@/components/layout'

const Page: React.FC = () => {
  return (
    <Layout>
      <PageHeader />
      <AddAccountFrom />
    </Layout>
  )
}

export default Page
