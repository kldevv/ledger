import { Layout } from '@/components/layout';

const Page: React.FC = () => {
  return (
    <Layout>
      <h1 className="text-darkShades font-extrabold text-3xl">Entries</h1>
    </Layout>
  );
};

export default Page;
